{
    "name": "vite-typescript-starter",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "cross-env TAILWIND_MODE=watch && tsc --watch & vite",
        "build": "cross-env TAILWIND_MODE=build && tsc && vite build",
        "preview": " npm run build && vite preview",
        "pages": "npm run build && gh-pages -d public"
    },
    "devDependencies": {
        "@types/d3": "^7.4.0",
        "autoprefixer": "^10.4.13",
        "cross-env": "^7.0.3",
        "gh-pages": "^4.0.0",
        "tailwindcss": "^3.2.4",
        "typescript": "^4.9.3",
        "vite": "^4.0.4"
    },
    "dependencies": {
        "d3": "^7.8.2"
    }
}
